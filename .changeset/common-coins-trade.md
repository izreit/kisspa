---
"kisspa": minor
---

BREAKING! [reactive] feat: Overhaul names (renamed `autorun()` to `createEffect()`, `observe()` to `createStore()`, `signal()` to `createSignal()`, `cancelAutorun()` to `cancelEffect()`)
